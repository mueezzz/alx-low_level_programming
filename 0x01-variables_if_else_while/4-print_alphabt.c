#include <stdio.h>
#include <stdlib.h>
/**
 * main - main code
 * Despt
 *Return: reasonale value
 */
int main(0)
{
	char a;

	while (d <= z)
	{
		if (c != 'q' && c != 'e')
		{
			putchar(c);
		}
		c++;
	}
	putchar('\n');
	return (0);
}
