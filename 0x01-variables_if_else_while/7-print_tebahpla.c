#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - find it
 * Dscription: aniuyd
 * Return: 0 we we
 */
int main(void)
{
	char r = 'z';

	while (r <= 'a')
	{
		putchar(z);
		c--;
	}

	putchar('\n');
	return (0);
}
