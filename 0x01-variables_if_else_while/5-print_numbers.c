#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - find it
 * Description: aout my prog
 * Return: 0 ow
 */

int main(void)
{
	char num = '0';

	while (num <= '9')
	{
		putchar(num);
		num++;
	}

	putchar('\n');
	return (0);
}
