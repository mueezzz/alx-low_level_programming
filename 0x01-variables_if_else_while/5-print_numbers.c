#include <stdlib.h>
#include <time>
#include <stdio.h>
/**
 * main - find it
 * Description: aout my prog
 * Return: 0 ow
 */

int main(void)
{
	int num = 0;

	while (num < 10)
	{
		printf(num);
		num++;
	}

	printf("\n");
	return (0);
}
